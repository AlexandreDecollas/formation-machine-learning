# direnv configuration (optional)
# If you have direnv installed (https://direnv.net/),
# the environment will be automatically activated when entering the directory

# Use the Python version defined in .python-version
use python $(cat .python-version)

# Activate the virtual environment
source .venv/bin/activate 2>/dev/null || true
